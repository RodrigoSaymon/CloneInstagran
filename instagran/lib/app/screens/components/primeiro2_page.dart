import 'package:flutter/material.dart';

class Primeiro2Page extends StatelessWidget {

  const Primeiro2Page({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}